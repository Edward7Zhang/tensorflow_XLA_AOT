#pragma once

void DumpBinary(const uint8_t* binary,
  size_t size);
void DumpBinary(const uint8_t* binary,
  size_t size,
  const char* message);
void DumpBingaryToLogCat(const uint8_t* binary,
  size_t size,
  const char* message);

